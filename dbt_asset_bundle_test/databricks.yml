# This file defines the structure of this project and how it is deployed
# to production using Databricks Asset Bundles.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbt_asset_bundle_test

include:
  - resources/*.yml

# Deployment targets.
# The default schema, catalog, etc. for dbt are defined in dbt_profiles/profiles.yml
targets:
  dev:
    default: true
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    workspace:
      host: https://adb-872699940352349.9.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-872699940352349.9.azuredatabricks.net
      # We explicitly specify /Users/18e266c0-5406-4f5a-8018-bf60c50285b1 to make sure we only have a single copy.
      root_path: /Users/18e266c0-5406-4f5a-8018-bf60c50285b1/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - service_principal_name: 18e266c0-5406-4f5a-8018-bf60c50285b1
        level: CAN_MANAGE
    run_as:
      service_principal_name: 18e266c0-5406-4f5a-8018-bf60c50285b1
